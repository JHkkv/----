# 初步学习使用 Markdown 格式

- "#" 以这个作为标题来进行说明，增加几个就是几级标题
- "-" 通过这个可以作为分级标题
- ">" 表示引用
- "**"表示强调

---

# 初步使用，设定一个关于处理数据的Agent工作流

- 在提供一份数据时，首先**对数据进行加密处理**，设定只能由当前这个Agent可以查看和修改
- 数据的修改流程需要进行**逐步自检**，并且要对原始数据进行单独保存
- 数据的展示，可以使用多种方式呈现：图表、线性表等，可以先提前询问
- 处理类似的数据时，自动调用这个Agent
